name: smart_labs_analyzer
description: Smart Labs Analyzer mobile scaffold for Qwen Build Day 2026
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  crypto: ^3.0.3
  file_picker: ^8.1.2
  http: ^1.2.2

  # Keep dependencies focused on upload and analysis-panel UX flows.

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
