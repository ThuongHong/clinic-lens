name: smart_labs_analyzer
description: Smart Labs Analyzer mobile scaffold for Qwen Build Day 2026
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2

  # Add flutter_scene once the team pins the exact package version and asset pipeline.

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
